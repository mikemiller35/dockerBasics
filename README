- Simple repo to show the basics of Docker

`python3 -m venv venv`

`source venv/bin/activate`

`pip install --upgrade pip; pip install --user -r requirements.txt`

`flask run`

---

`docker-compose -f docker-compose.yml up -d --build`

`docker-compose ps`

`docker-compose down`
